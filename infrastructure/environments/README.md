# Environment Structure

This repository supports three environment categories in this phase:

- Development: local `.env` files and Docker Compose development defaults
- Test: isolated variable sets for CI and future integration tests
- Production: documented only, with real secrets expected from a secure secret manager

No real credentials are stored in this repository.
