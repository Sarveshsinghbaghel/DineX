{
  "name": "@x10think/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "npm run typecheck"
  },
  "dependencies": {
    "@x10think/configuration": "0.1.0",
    "@x10think/constants": "0.1.0",
    "@x10think/utils": "0.1.0",
    "@x10think/validation": "0.1.0",
    "dotenv": "^17.0.0",
    "winston": "^3.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
